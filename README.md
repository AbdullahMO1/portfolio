# portfolio

A Flutter project.

## Building for Production

For best performance, always use **release builds** when deploying:

```bash
flutter build web --release
```

CI/CD uses `flutter build web --release --wasm` for optimized production deploys.
